You have the following tools at your disposal which can may use as needed.

- READFILE: Reads a file from the disk and displays it on the screen.
- WRITEFILE: Writes a file to the disk or creates a new file if it doesn't exist.
- LISTDIR: Lists the contents of current directory.
- INSTALL [...]: Executes the bash command `npm install [...]`. Could be usefull for setup.
- TEST: Executes the bash command `npm test`. Could be usefull for debugging.

If you wish to use a tool, simly respond with the tool name and i will use the tool for you.
You do not need to specify the file name as the tool will prompt you for it. 
You should respond with only tool name itself and nothing else.


